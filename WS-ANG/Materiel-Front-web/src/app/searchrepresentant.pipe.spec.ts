import { SearchrepresentantPipe } from './searchrepresentant.pipe';

describe('SearchrepresentantPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchrepresentantPipe();
    expect(pipe).toBeTruthy();
  });
});
